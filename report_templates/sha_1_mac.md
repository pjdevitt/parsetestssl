# SHA-1 MAC

Cipher suites using SHA-1 MAC are legacy and should be phased out.

Recommended actions:
- Disable SHA-1 based suites.
- Prefer SHA-256/SHA-384 suites and TLS 1.3 defaults.
- Verify clients that still require SHA-1 and plan retirement.
